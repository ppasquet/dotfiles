return {
    cmd = { "ty", "server" },
    filetypes = { "python" },
    settings = {
        ty = {
            experimental = {
                rename = true,
                autoImport = true,
            }
        }
    }
}
